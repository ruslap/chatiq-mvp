# ============================================
# CHTQ Production Environment Variables
# ============================================
# Copy this file to .env and fill in the values
# NEVER commit .env to git!
# ============================================

# ==========================================
# PostgreSQL Database
# ==========================================
POSTGRES_USER=chtq_user
POSTGRES_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD_32_CHARS
POSTGRES_DB=chtq_production

# Database URL (used by API server)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://chtq_user:CHANGE_ME_USE_STRONG_PASSWORD_32_CHARS@postgres:5432/chtq_production

# ==========================================
# Redis Cache
# ==========================================
# Redis URL for caching and session storage
REDIS_URL=redis://redis:6379

# ==========================================
# API Server (NestJS)
# ==========================================
# JWT secret for authentication tokens (generate with: openssl rand -base64 64)
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_64_CHARS

# Google OAuth credentials
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://api.chtq.ink/auth/google/callback
FRONTEND_URL=https://admin.chtq.ink

# Public accessible API URL (for file uploads etc)
PUBLIC_API_URL=https://api.chtq.ink

# ==========================================
# Admin Panel (Next.js)
# ==========================================
# Public API URL (must be accessible from browser)
NEXT_PUBLIC_API_URL=https://api.chtq.ink

# NextAuth configuration
NEXTAUTH_URL=https://admin.chtq.ink
NEXTAUTH_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_32_CHARS

# ==========================================
# Security Notes
# ==========================================
# Generate secure secrets with:
#   openssl rand -base64 64    # For JWT_SECRET
#   openssl rand -base64 32    # For NEXTAUTH_SECRET, POSTGRES_PASSWORD
#
# Google OAuth setup:
# 1. Go to Google Cloud Console
# 2. Create OAuth 2.0 credentials
# 3. Add authorized redirect URIs:
#    - https://api.chtq.ink/auth/google/callback (Production)
#    - http://localhost:3000/auth/google/callback (Local Development)
# ============================================
